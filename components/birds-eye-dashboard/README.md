# Shared review dashboard

This directory is an unchanged release snapshot of `birds_eye_review_code/packages/dashboard-ui/src`, used by the validated nattokinase dashboard. Scientific selection is computed by the pipeline, never re-derived here. Sync from that canonical package rather than patching this snapshot independently. `source-manifest.json` records each source file hash. Historical review pages retain their existing components.
